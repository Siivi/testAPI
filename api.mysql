CREATE TABLE `markers`.`my_favorite` (
  `id` INT NOT NULL,
  `title` VARCHAR(60) NOT NULL,
  `image` VARCHAR(260) NOT NULL,
  `descrioption` VARCHAR(60) NULL,
  `published` INT NOT NULL,
  `publisedEst` INT NULL,
  UNIQUE INDEX `id_UNIQUE` (`id` ASC));
